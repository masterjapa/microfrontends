{
  "private": true,
  "scripts": {
    "ls": "lerna ls",
    "start": "lerna run --scope @mf/* --parallel start:lerna",
    "build": "lerna run --scope @mf/* build",
    "serve": "lerna run --scope @mf/* --parallel serve",
    "clean": "lerna run --scope @mf/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
